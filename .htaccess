RewriteEngine On

# Strip /eserbisyo-hub/ prefix from any incoming request
RewriteRule ^eserbisyo-hub/(.*)$ /$1 [L,R=301]

# Redirect bare root to kiosk landing
RewriteRule ^$ /client/kiosk/pages/index.html [L,R=302]